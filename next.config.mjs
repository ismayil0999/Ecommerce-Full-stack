/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com'], // İzin verilen alan adlarını buraya ekleyin
  },
  reactStrictMode: true,
};

export default nextConfig;
